name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-recovery-${{ github.sha }}"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate chapter payloads
        run: python tools/validate_library.py

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

# deployment trigger: episode 10 / 2026-09-07
# deployment trigger: episode 10 dialogue reflow / 2026-09-07
# deployment trigger: episode 10 dialogue reflow v3 / 2026-09-07
# deployment trigger: episode 10 punctuation v9 / 2026-09-08
# deployment trigger: restored mirror metadata + episode 10 punctuation v9 / 2026-09-08
# deployment trigger: rollback episode 10 to v8 / 2026-09-08
# deployment trigger: episode 11 / 2026-09-08
# deployment trigger: calibrated episode 11 rewrite / 2026-09-08
# deployment trigger: episode 11 dialogue v4 / 2026-09-08
# deployment trigger: episode 11 literary rewrite + normal dialogue rendering / 2026-09-08
# deployment trigger: episode 11 literary v6 mirror / 2026-09-08
# deployment trigger: episode 13 mirror / 2026-09-10
# deployment trigger: episode 14 mirror / 2026-09-11
# deployment trigger: episode 15 mirror / 2026-09-12
